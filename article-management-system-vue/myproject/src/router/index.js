import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router)

const index=()=>import('@/components/pages/index.vue')

export default new Router({
    routes:[
        {
            path:'/',
            component:
        }
    ]
})